<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose drives the real UI; this base theme sets a black, no-action-bar window
         with white status/nav icons, matching the Light Phone III look. -->
    <style name="Theme.LifeGroupTexts" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>

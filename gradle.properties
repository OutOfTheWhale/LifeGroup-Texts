# NOTE: build with a JDK 17-21 (e.g. the one bundled in Android Studio). If building
# from the command line, either build via Android Studio, set JAVA_HOME to a JDK 17-21,
# or set `org.gradle.java.home` in your user-level ~/.gradle/gradle.properties (kept out
# of version control because it is machine-specific).

org.gradle.jvmargs=-Xmx2560m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
